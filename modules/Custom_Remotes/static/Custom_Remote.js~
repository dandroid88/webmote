function remoteButton(id) {
    if ($('#edit_remote_slider').find("option:selected").val() == 'on') {
        window.location = '/button/' + id + '/';
    } else {
        $.ajax({
            url: '/run_button/' + id + '/',
            type: 'GET',
        });
    }
}
